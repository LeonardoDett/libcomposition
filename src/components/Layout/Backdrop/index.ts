import { BackdropRoot } from "./BackdropRoot";
import { BackdropContent } from "./BackdropContent";
import { BackdropTrigger } from "./BackdropTrigger";

export const Backdrop = {
    Root: BackdropRoot,
    Content: BackdropContent,
    Trigger: BackdropTrigger,
};